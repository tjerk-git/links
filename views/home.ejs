<!DOCTYPE html>
<html>
<head>
    <title>Links</title>
    <link rel="stylesheet" href="https://fonts.bunny.net/css">
    <style>
        :root {
            --pink: #FF79A4;
            --black: #2F2F2F;
            --off-white: #F4EFEF;
            --light-pink: #F4CDCD;
            --purple: #A55C8E;
            --font-family: 'Inter', sans-serif;
            --background: #fff3fd;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-family);
            background: var(--background);
            margin: 0;
            padding: 0;
            font-size: 1.25rem;
            line-height: 1.5;
        }

        main {
            padding: 2rem 1rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            font-size: 3rem;
            color: var(--black);
            margin-bottom: 2rem;
            font-weight: 400;
        }

        .playlist-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 1.4rem;
            background: var(--off-white);
            border-radius: 8px;
            overflow: hidden;
        }

        .playlist-table tr {
            height: 5rem;
            transition: all 0.2s ease;
        }

        .playlist-table tr:nth-child(odd) {
            background: rgba(0, 0, 0, 0.03);
        }

        .playlist-table th,
        .playlist-table td {
            padding: 1rem 2rem;
            font-weight: 400;
            text-align: left;
        }

        .playlist-table th {
            background: var(--purple);
            color: var(--off-white);
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .playlist-table td a {
            color: var(--purple);
            text-decoration: none;
            transition: color 0.2s ease;
        }

        .playlist-table td a:hover {
            color: var(--pink);
        }

        .playlist-table tr:hover {
            background: rgba(0, 0, 0, 0.05);
            cursor: pointer;
        }

        .date-column {
            white-space: nowrap;
            color: var(--black);
            opacity: 0.7;
        }

        @media (max-width: 768px) {
            body {
                font-size: 1rem;
            }

            h1 {
                font-size: 2rem;
                padding: 0 1rem;
            }

            .playlist-table {
                font-size: 1rem;
            }

            .playlist-table th,
            .playlist-table td {
                padding: 0.75rem 1rem;
            }

            .date-column {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <main>
        <h1>Tjerk's Links</h1>
        <table class="playlist-table">
            <thead>
                <tr>
                    <th>Link</th>
                    <th>Added</th>
                </tr>
            </thead>
            <tbody>
                <% links.forEach(function(link) { %>
                    <tr>
                        <td><a href="<%= link.url %>" target="_blank"><%= link.url %></a></td>
                        <td class="date-column"><%= new Date(link.date_added).toLocaleDateString('en-US', { 
                            year: 'numeric', 
                            month: 'short', 
                            day: 'numeric',
                            hour: '2-digit',
                            minute: '2-digit'
                        }) %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </main>
</body>
</html>
